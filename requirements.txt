# Python tooling only -- the engine itself has zero runtime dependencies.
# These power weight export and the reference-diffing verification harness.

numpy>=1.24
torch>=2.0
transformers>=4.30
sentencepiece>=0.1.99   # TinyLlama 1.1B SentencePiece tokenizer (convert_tokenizer.py --arch llama)

# tiktoken>=0.5         # only needed for a tiktoken-style tokenizer (e.g. Llama 3.2)
